# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Analysis outputs from real customer data — keep local only
scripts/*.json
scripts/*.csv

# Raw customer data exports (PII: caller numbers, transcripts) — NEVER commit
# Anchored to repo root so it does NOT match src/data (app code).
/data/
etl/.env
etl/node_modules/
probe-service/node_modules/
probe-service/.env
probe-service/.token

# Local environment variables
.env
.env.local
.env.*.local
