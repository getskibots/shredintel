# Shared secret. The ShredIntel proxy sends `Authorization: Bearer <this>`.
# Must equal PROBE_SERVICE_TOKEN in Vercel. Generate one: openssl rand -hex 32
PROBE_TOKEN=

# Port the service listens on (default 8080).
PORT=8080
